# Evidence-first onboarding

Implement the 13 September mechanism proposal against PR 214. Preserve workspace isolation, fail-closed scheduling, approval-before-publishing, unknown metrics, cached decisions and persistent runs.

Delivery: metric contracts and scoped provider budgets/accounting; focused profile and capability provenance; structured seed families and bounded semantic/competitor sources; diverse candidate shortlist; auditable qualification with page-intent checks and bounded continuation; approved-output checks; choose a supported first brief before writing; draft before trial. Reuse research evidence and remove avoidable critical-path work.

Validation: deterministic provider fixtures through production selection, focused and full unit tests, type/lint/build, isolated browser journey. Record actual provider samples separately from fixtures and human labels; do not claim conversion improvements from test passes. Hosted Stripe sandbox verification requires a real isolated test configuration; never exercise live checkout as a substitute.
